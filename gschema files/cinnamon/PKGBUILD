# Maintainer: Keegan Milsten <willnhalt@gmail.com>

pkgname=reborn-schemas-gnome
_pkgname=reborn-schemas-gnome
pkgver=1.1
pkgrel=1.1
pkgdesc="Setup configuration for Gnome Reborn"
url="https://github.com/keeganmilsten/reborn-schemas/tree/master/cinnamon"
arch=('any')
license=('GPL2')
depends=('gsettings-desktop-schemas' 'dconf')
#replaces=('antergos-desktop' 'antergos-gsettings')
source=("reborn-schemas-${pkgver}.tar.gz::https://codeload.github.com/keeganmilsten/reborn-schemas/tar.gz/${pkgver}")
install=reborn-schemas/cinnamon/schema.install
package() {
    cd "${srcdir}/${_pkgname}-${pkgver}"
    install -d "${pkgdir}/usr/share/glib-2.0/schemas"
    install -Dm644  /cinnamon/95_cinnamon.gschema.override  \
        "${pkgdir}/usr/share/glib-2.0/schemas/95_cinnamon.gschema.override"
}
